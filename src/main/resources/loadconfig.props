# Url
baseUrl=http://localhost:8080/portal/auth/portal/opus/default
# Maximum load
maxLoad=1
# In secs
runTime=60
# Terminate Immediately
terminateNow=false
# Test Script path - Should be in resource folder
data=testJson.json
pageWait=100
visible=false